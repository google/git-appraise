{"timestamp":"1450364145","targetRef":"refs/heads/master","reviewRef":"refs/pull/12/head","requester":"hazbo","description":"Force default git editor when omitting -m\nFor review comments, the absense of the -m flag will now attempt to load the\r\nuser's default git editor.\r\n\r\ni.e. git appraise comment c0a643ff39dd\r\n\r\nAn initial draft as discussed in #8 \r\n\r\nI'm still not sure whether or not the file that is saved is in the most appropriate place or not. I like the idea of it being relative to the project although it could have gone in `/tmp` I suppose."}
{"timestamp":"1450364829","targetRef":"refs/heads/master","reviewRef":"refs/pull/12/head","requester":"hazbo","description":"Force default git editor when omitting -m\nFor review comments, the absense of the -m flag will now attempt to load the\r\nuser's default git editor.\r\n\r\ni.e. git appraise comment c0a643ff39dd\r\n\r\nAn initial draft as discussed in #8 \r\n\r\nI'm still not sure whether or not the file that is saved is in the most appropriate place or not. I like the idea of it being relative to the project although it could have gone in `/tmp` I suppose."}
{"timestamp":"1450378254","targetRef":"refs/heads/master","reviewRef":"refs/pull/12/head","requester":"hazbo","description":"Force default git editor when omitting -m\nFor review comments, the absense of the -m flag will now attempt to load the\r\nuser's default git editor.\r\n\r\ni.e. git appraise comment c0a643ff39dd\r\n\r\nAn initial draft as discussed in #8 \r\n\r\nI'm still not sure whether or not the file that is saved is in the most appropriate place or not. I like the idea of it being relative to the project although it could have gone in `/tmp` I suppose."}
{"timestamp":"1450381010","targetRef":"refs/heads/master","reviewRef":"refs/pull/12/head","requester":"hazbo","description":"Force default git editor when omitting -m\nFor review comments, the absense of the -m flag will now attempt to load the\r\nuser's default git editor.\r\n\r\ni.e. git appraise comment c0a643ff39dd\r\n\r\nAn initial draft as discussed in #8 \r\n\r\nI'm still not sure whether or not the file that is saved is in the most appropriate place or not. I like the idea of it being relative to the project although it could have gone in `/tmp` I suppose."}
{"timestamp":"1450381228","targetRef":"refs/heads/master","reviewRef":"refs/pull/12/head","requester":"hazbo","description":"Force default git editor when omitting -m\nFor review comments, the absense of the -m flag will now attempt to load the\r\nuser's default git editor.\r\n\r\ni.e. git appraise comment c0a643ff39dd\r\n\r\nAn initial draft as discussed in #8 \r\n\r\nI'm still not sure whether or not the file that is saved is in the most appropriate place or not. I like the idea of it being relative to the project although it could have gone in `/tmp` I suppose."}
{"timestamp":"1450385360","targetRef":"refs/heads/master","reviewRef":"refs/pull/12/head","requester":"hazbo","description":"Force default git editor when omitting -m\nFor review comments, the absense of the -m flag will now attempt to load the\r\nuser's default git editor.\r\n\r\ni.e. git appraise comment c0a643ff39dd\r\n\r\nAn initial draft as discussed in #8 \r\n\r\nI'm still not sure whether or not the file that is saved is in the most appropriate place or not. I like the idea of it being relative to the project although it could have gone in `/tmp` I suppose."}
{"timestamp":"1450385379","targetRef":"refs/heads/master","reviewRef":"refs/pull/12/head","requester":"hazbo","description":"Force default git editor when omitting -m\nFor review comments, the absense of the -m flag will now attempt to load the\r\nuser's default git editor.\r\n\r\ni.e. git appraise comment c0a643ff39dd\r\n\r\nAn initial draft as discussed in #8 \r\n\r\nI'm still not sure whether or not the file that is saved is in the most appropriate place or not. I like the idea of it being relative to the project although it could have gone in `/tmp` I suppose."}
{"timestamp":"1450390235","targetRef":"refs/heads/master","reviewRef":"refs/pull/12/head","requester":"hazbo","description":"Force default git editor when omitting -m\nFor review comments, the absense of the -m flag will now attempt to load the\r\nuser's default git editor.\r\n\r\ni.e. git appraise comment c0a643ff39dd\r\n\r\nAn initial draft as discussed in #8 \r\n\r\nI'm still not sure whether or not the file that is saved is in the most appropriate place or not. I like the idea of it being relative to the project although it could have gone in `/tmp` I suppose."}

{"timestamp":"1450390235","reviewRef":"refs/pull/12/head","targetRef":"refs/heads/master","requester":"hazbo","description":"Force default git editor when omitting -m\n\nFor review comments, the absense of the -m flag will now attempt to load the\r\nuser's default git editor.\r\n\r\ni.e. git appraise comment c0a643ff39dd\r\n\r\nAn initial draft as discussed in #8 \r\n\r\nI'm still not sure whether or not the file that is saved is in the most appropriate place or not. I like the idea of it being relative to the project although it could have gone in `/tmp` I suppose.","baseCommit":"00c0e827e5b86fb9d200f474d4f65f43677cbc6c"}

{"timestamp":"1450390235","reviewRef":"refs/pull/12/head","targetRef":"refs/heads/master","requester":"hazbo","description":"Force default git editor when omitting -m\n\nFor review comments, the absense of the -m flag will now attempt to load the\nuser's default git editor.\n\ni.e. git appraise comment c0a643ff39dd\n\nAn initial draft as discussed in #8 \n\nI'm still not sure whether or not the file that is saved is in the most appropriate place or not. I like the idea of it being relative to the project although it could have gone in `/tmp` I suppose.\n","baseCommit":"00c0e827e5b86fb9d200f474d4f65f43677cbc6c"}
